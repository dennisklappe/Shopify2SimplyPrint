# @remix-run/dev declares an optional peer dependency on wrangler v3, while this
# project runs wrangler v4. The two are compatible in practice, but npm's
# resolver rejects the combination by default and fails `npm ci`, including in
# Cloudflare's build step. Accept the optional peer instead.
legacy-peer-deps=true
